# Agent Operating Model

`AGENTS.md` is the entry point for repository-wide agent instructions. `BASELINE.md` contains the non-negotiable engineering and product standards. Phase-specific instructions should be added here only when they do not belong in the source-of-truth architecture or product documentation.

Agents must leave the worktree uncommitted and report validation results, unresolved risks, and any files that contain local-only data.
